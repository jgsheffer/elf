import { StyleSheet} from "react-native";

interface  LocationZoomViewProps
{
  currentQuestionIndex : number,
  totalQuestions : number
}

export const LocationZoomView: React.FC<LocationZoomViewProps> = ({ currentQuestionIndex, totalQuestions }) => {
  return null
};

const styles = StyleSheet.create({

});